# Pre-launch gate is active — block crawlers entirely while we're behind the
# preview-token gate. Delete or replace this file at launch when the gate
# in functions/_middleware.js is removed.
User-agent: *
Disallow: /
